export interface HoodTradeTy {
  activity_date: string;
  process_date: string;
  settle_date: string;
  symbol: string;
  description: string;
  trans_code: string;
  quantity: number;
  price: number;
  amount: number;
};
